import { StoryObj, Meta } from '@storybook/react';
import UptoDateSection from '.';

export default {
  title: 'Molecules/UptoDateSection',
  component: UptoDateSection,
} as Meta;
type Story = StoryObj<typeof UptoDateSection>;

export const Default: Story = {
  args: {},
};
